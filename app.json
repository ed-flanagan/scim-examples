{
  "name": "1Password SCIM Bridge",
  "description": "1Passwords SCIM Bridge for Business accounts",
  "keywords": [
    "1password",
    "scim"
  ],
  "logo": "https://i.1password.com/media/buttons-and-icons/scim-bridge.svg",
  "repository": "https://github.com/1Password/scim-examples",
  "stack": "container",
  "formation": {
    "web": {
      "quantity": 1
    }
  },
  "env": {
    "OP_REDIS_INSECURE_SSL": {
      "description": "Set whether insecure TLS connections to Redis should be permitted when `OP_REDIS_SSL_ENABLED` is set to '1'. (Boolean 0 or 1)",
      "value": 1,
      "required": false
    },
    "OP_REDIS_SSL_ENABLED": {
      "description": "Set whether the connection to Redis should be TLS-enabled (Boolean 0 or 1)",
      "value": 1,
      "required": false
    },
    "OP_SESSION": {
      "description": "Base64-encoded scimsession file 1Password provides you",
      "required": true
    },
    "OP_WORKSPACE_CREDENTIALS": {
      "description": "Google Workspace only: base64-encoded credentials",
      "required": false
    },
    "OP_WORKSPACE_SETTINGS": {
      "description": "Google Workspace only: base64-encoded setttings",
      "required": false
    }
  },
  "addons": [
    {
      "plan": "heroku-redis:premium-0",
      "as": "OP_REDIS",
      "options": {
        "maxmemory_policy": "volatile-lru"
      }
    }
  ]
}
